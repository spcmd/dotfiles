#                               _
#  ___ _ __   ___ _ __ ___   __| |
# / __| '_ \ / __| '_ ` _ \ / _` |
# \__ | |_) | (__| | | | | | (_| |
# |___| .__/ \___|_| |_| |_|\__,_|
#     |_|
# Created by: spcmd
# http://spcmd.github.io
# https://github.com/spcmd

rsync-320GB_Rack() {
    RACK_BACKUP_DIR='/media/320GB_Rack/00Backup_spcmd/'
    if [[ -d $RACK_BACKUP_DIR ]]; then
        rsync -avr $HOME/Documents/ $RACK_BACKUP_DIR/Documents/
        rsync -avr $HOME/.password-store/ $RACK_BACKUP_DIR/.password-store
        rsync -avr $HOME/.spotymenu/ $RACK_BACKUP_DIR/.spotymenu
        rsync -avr $HOME/.dsnippet/ $RACK_BACKUP_DIR/.dsnippet
        rsync -avr $HOME/Calibre\ Library/ $RACK_BACKUP_DIR/Calibre\ Library
    else
        echo "==> Error: \033[1;34m$RACK_BACKUP_DIR \033[0mnot found. Is it mounted? Or has the directory changed?"
    fi
}

rsync-Pendrive() {
    PENDRIVE='/media/PENDRIVE1GB'
    if [[ -d $PENDRIVE ]]; then
        rsync -avr $HOME/.password-store/ $PENDRIVE/.password-store
        rsync -avr $HOME/.spotymenu/ $PENDRIVE/.spotymenu
        rsync -avr $HOME/.dsnippet/ $PENDRIVE/.dsnippet
        rsync -avr $HOME/Documents/tuxwiki $PENDRIVE
        rsync -avr $HOME/Documents/Notes $PENDRIVE
        rsync -avr $HOME/Documents/TODO.todo $PENDRIVE
    else
        echo "==> Error: \033[1;34m$PENDRIVE \033[0mnot found. Is it mounted? Or has the name changed?"
    fi
}

# vim: set syntax=sh filetype=sh :
